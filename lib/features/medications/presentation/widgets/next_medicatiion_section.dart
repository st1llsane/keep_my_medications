import 'package:flutter/material.dart';

class NextMedicatiionSection extends StatelessWidget {
  const NextMedicatiionSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
